# Code-Review `sources/*.js`

Stand: 2026-09-04
Repository: https://github.com/seanox/composite-js/tree/master/sources
Umfang (~6.000 Zeilen):
- composer.js
- expression.js
- reactive.js
- routing.js
- datasource.js
- extension.js
- messages.js
- scripting.js
- test.js

Aufwand in Story Points (1/2/3/5/8/13).
Alle Findings wurden im Code verifiziert; modulübergreifende Dubletten sind
zusammengefasst.

## Hoch

| #     | Problem                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Stelle                                                                                         | SP |
|-------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------|----|
| 1     | __Code-Injection über `String.prototype.unescape`__: `.replace(/^(["'])/, "\$1")` ist ein No-op, das führende Anführungszeichen wird nie escaped. i18n-Werte aus `locales.xml`/Modul-XML landen ungeschützt in `eval` (`"+(alert(1))//`).                                                                                                                                                                                                                           | extension.js:537-539, messages.js:49                                                           | 3  |
| ~~2~~ | __Syntaxfehler in generierter Route-Expression__: `${composite}")` ohne öffnendes `"` (vgl. Z. 482). Composite mit `route` __und__ eigener `condition` erzeugt ungültige Expression und verschwindet immer.                                                                                                                                                                                                                                                         | routing.js:478                                                                                 | 1  |
| 3     | __XSS-Oberfläche__: `output` schreibt Expression-Ergebnisse per `innerHTML` (Doku sagt „als Text“), ebenso `import`/`include` ohne Sanitizing. XSLT-Output setzt `innerHTML = textContent` und schreibt `<script>` in `composite/javascript` um, das der Renderer ausführt.                                                                                                                                                                                         | composer.js:2438, 2375, 1528; datasource.js:195-215                                            | 5  |
| ~~4~~ | __Jede `{{…}}`-Sequenz im DOM ist ausführbarer JS-Code__ (`Function` + `eval`, kein Escaping, kein Opt-out). Nutzerdaten mit `{{` = Remote Code Execution. Framework ist nicht CSP-fähig (`unsafe-eval` in Scripting, `compliant`, `unescape`).                                                                                                                                                                                                                     | expression.js:66; scripting.js:225-232; extension.js:52-63, 307-317                            | 8  |
| 5     | __Synchrones XHR auf dem Main-Thread__ in Bootstrap, `load`, `import`, DataSource, Messages. Deprecated, blockiert UI; `status 0` (file://, CORS, offline) bricht die gesamte Framework-Initialisierung ab. Netzwerkfehler nicht abgefangen.                                                                                                                                                                                                                        | composer.js:1377, 2384, 2861; datasource.js:286, 437-449; messages.js:100-109                  | 8  |
| 6     | __Speicherlecks / unbegrenzt wachsender Zustand__: `Composer.mount.stack` (Array, `includes` O(n), nie bereinigt); `_render_meta` (sparse Array mit starken Element-Refs, Textknoten-Meta ohne `parentNode` nie aufgeräumt); Expression-`_cache` (Map `serial:attr`, nie geleert); Reactive-`notifications` (DOM-Refs pro Key, Cleanup nur im `set`-Trap desselben Keys). `_lock.release` mountet bei jedem Render-Ende alle `querySelectorAll("*")` → quadratisch. | composer.js:730-731, 812, 1873, 2269, 2927; expression.js:60-75; reactive.js:117, 119, 182-213 | 8  |
| 7     | __Reactive-Proxy zerstört Objekte mit internen Slots__ (Map, Set, Date, WeakMap, Promise, TypedArrays): Methoden werden mit `this = proxy` aufgerufen → `TypeError: incompatible receiver`. Nur `Node`/`NodeList`/`HTMLCollection` ausgenommen.                                                                                                                                                                                                                     | reactive.js:121-163                                                                            | 3  |
| 8     | __Tolerant-Makro `(?…)` korrumpiert Regex-Literale__: `/(?:ab)+/` → `/(_tolerate(()=>:ab))+/`. Regex-Literale werden im Parser nicht als Literal erkannt; `/["]/` schaltet in String-Modus und verschluckt nachfolgende `#export`-Makros. Gleiche Erkennung in expression.js trifft `(?:…)`.                                                                                                                                                                        | scripting.js:94-141; expression.js:242-252                                                     | 8  |
| 9     | __Doppel-Encoding + `"undefined"`__: Attributwert wird `encodeHtml()` + `&quot;` kodiert und per `setAttribute`/Property gesetzt (beide dekodieren nicht) → `title="{{'a<b'}}"` ergibt literal `a&lt;b`. Bei `undefined` wird Attribut entfernt, danach aber `selector[attribute] = undefined` → `input.value === "undefined"`.                                                                                                                                     | composer.js:2608-2633                                                                          | 2  |
| 10    | __Test-Framework-Deadlock__: Wirft ein `Test.listen`-Callback bei `EVENT_RESPONSE`, wird `queue.lock = false` nie erreicht → Testlauf hängt; Timeout-Interval feuert RESPONSE alle 25 ms endlos.                                                                                                                                                                                                                                                                    | test.js:305-307, 340-374                                                                       | 2  |

## Mittel

| #      | Problem                                                                                                                                                                                                                                                                                                                                                                                                            | Stelle                                                                                        | SP |
|--------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------|----|
| 11     | __Enumerable Prototype-Erweiterungen__ per Zuweisung statt `defineProperty({enumerable:false})`: `Object.prototype.ordinal/reactive/toPlainString`, `String.prototype.*`, `Element.prototype.*` erscheinen in jedem `for…in`. `ordinal()` gibt vorhandenes eigenes `serial`-Feld zurück (Kollision). `compliant` wirft hart bei künftigen Standard-Kollisionen → Framework stirbt beim Laden statt zu degradieren. | composer.js:2697-2703; reactive.js:76; datasource.js:407; test.js:689-695; extension.js:409ff | 3  |
| 12     | __Iterate ohne Diffing__: jeder Render-Zyklus leert `innerHTML` und baut alle Kinder neu (neue Serials, Meta, Cache-Einträge, Mount-Stack, Listener). Skaliert schlecht, verstärkt #6. Keyed Reconciliation ist ein Architekturvorhaben.                                                                                                                                                                           | composer.js:2494-2563                                                                         | 13 |
| 13     | __Kein Function-Cache__: pro Expression-Auswertung `Function(...)` + `eval`, `Composer.render.context` kopiert bei jedem Zugriff Scope + Workspace. „Duplicate parameter name“ wenn Iterationsvariable `script`, `_import`, `_export`, `_use`, `_tolerate` heißt. `_render_context_scope` nie befüllt (toter Code).                                                                                                | scripting.js:219-233; composer.js:59, 1537-1547                                               | 5  |
| 14     | __Render-Queue-Race__: bei Lock wird sowohl `asynchronous(render)` (ohne Selector → `queue[0]`) als auch beim Release `asynchronous(render, shift())` geplant → Doppel-Scheduling, Reihenfolge-Inversion; `render()` ohne Selector kann `undefined` in Queue pushen.                                                                                                                                               | composer.js:1178-1196, 1679-1681                                                              | 3  |
| 15     | __Fragiler Bootstrap__: wirft `Composite.include("common")` (HTTP ≠ 200/404), wird weder MutationObserver installiert noch gerendert – Seite bleibt leer ohne Fehlermeldung.                                                                                                                                                                                                                                       | composer.js:2841-2861, 3015                                                                   | 2  |
| 16     | Listener-Exceptions in `fire(EVENT_RENDER_END)` propagieren bis in MutationObserver/Event-Handler; `context.queue.shift()` wird übersprungen. Keine Isolation der Listener.                                                                                                                                                                                                                                        | composer.js:354-355, 1629-1684                                                                | 2  |
| 17     | `{{name:expr}}` in Textknoten schreibt __globale Variablen__ (`window[name]`) – Markup kann `Composer`, `Expression`, `location` überschreiben; kein Scope.                                                                                                                                                                                                                                                        | composer.js:2218                                                                              | 3  |
| 18     | Attributnamen pauschal `toLowerCase()` → SVG-camelCase-Attribute mit Expression (`viewBox`, `preserveAspectRatio`) werden als `viewbox` gesetzt und ignoriert.                                                                                                                                                                                                                                                     | composer.js:1949, 1962, 2629                                                                  | 2  |
| 19     | __Gecko-Workaround wirkungslos__: `Composer.asynchronous` ist Microtask, `hashchange` Macrotask → „Interrupt“ läuft immer vor dem Browser-Event. Bedingung `newURL !== Browser.location` ist invertiert. Toter/falscher Codepfad, potenziell Doppel-Dispatch.                                                                                                                                                      | routing.js:125-137, 321-324                                                                   | 3  |
| 20     | __Selector-Injection/Crash__: Hash-Segmente unescaped in `querySelector('[id="…"]')`; `PATTERN_PATH` erlaubt `"`, `]`, `\` → DOMException im hashchange-Listener. `newHash === null` (Navigation ohne `#`) → TypeError bei String-Interceptoren; RegExp mit `g`-Flag statusbehaftet.                                                                                                                               | routing.js:295-298, 336-352                                                                   | 2  |
| 21     | Reactive-Handler: `return true` im `finally` verschluckt Exceptions aus `target[key] = value` und rendert trotzdem; fehlende `deleteProperty`/`defineProperty`-Traps → kein Re-Render; `_selector` nach `EVENT_RENDER_NEXT` nie zurückgesetzt (falsche Recipient-Zuordnung); pro `get` Closure + Microtask auch außerhalb des Renderings (`_selector === null` erst im Callback geprüft).                          | reactive.js:55-56, 170-216, 240-272                                                           | 5  |
| ~~22~~ | __Regex-Präzedenzfehler__ `/^yes\|on\|true\|1$/i` = `^yes` oder `on` oder `true` oder `1$` → `escape="none"`, `"month"`, `"untrue"` gelten als true.                                                                                                                                                                                                                                                               | datasource.js:190, 197                                                                        | 1  |
| 23     | Async-Import-Callback greift auf ggf. per `_cleanup` gelöschtes `_render_meta[serial]` zu → TypeError. Import-Cache wird mit `responseURL` (absolut) befüllt, mit Roh-`value` (relativ) gelesen → nie Treffer. Kein Negative-Caching → bei Fehler sync XHR pro Render-Zyklus.                                                                                                                                      | composer.js:2374-2400                                                                         | 2  |
| 24     | `Namespace.use(object)` / `Namespace.create(object, value)` trotz dokumentierter Signatur kaputt (`"".split` → `[""]`, `eval("typeof [object Object]")`). Bei `null`-Wurzel (`window.a === null`) fällt `use("a.b")` auf globale Suche nach `b` zurück.                                                                                                                                                            | extension.js:108-193                                                                          | 3  |
| 25     | __Override von `Element.prototype.appendChild`__ ändert nativen Vertrag (2. Param `exclusive` löscht Kinder, Array statt Node als Rückgabe). `typeof node[Symbol.iterator]` ist immer truthy → `appendChild({})` liefert still `[]` statt TypeError. Nur `Element`, nicht `Node`/`DocumentFragment`.                                                                                                               | extension.js:326-342; composer.js:2366                                                        | 3  |
| ~~26~~ | `decodeHex` nutzt `text.substring(loop, 2)` (Ende-Index statt Länge) → `"ABC".encodeHex().decodeHex()` = `"A\u0000B"`. Asymmetrisch: `encodeHex` erzeugt 4 Stellen für Codepoints > 0xFF, `decodeHex` liest immer 2.                                                                                                                                                                                               | extension.js:455-456                                                                          | 1  |
| 27     | __Test-Framework ohne async-Unterstützung__: Rückgabewert (Promise) von `meta.test()` wird ignoriert → async Tests sofort „grün“, Rejections unhandled. Timeout-Interval ist faktisch tot, da synchrone Tests nicht unterbrochen werden.                                                                                                                                                                           | test.js:288-308, 338-375                                                                      | 5  |
| 28     | Messages: `populate` nutzt `replace(RegExp, values[index])` → `$&`, `$$`, `$'` werden als Replacement-Pattern interpretiert. Keys mit Nicht-`\w`-Zeichen (`foo-bar`) still verworfen. Key `populate` kollidiert mit Methode.                                                                                                                                                                                       | messages.js:53-85                                                                             | 2  |
| 29     | `responseXML === null` (Parse-Fehler, falscher MIME) wird gecached → jeder Folgeaufruf `null.clone()` TypeError; kein `<parsererror>`-Check. Cache-Key `hashCode()` (31er-Hash) kollisionsanfällig → falsche Dokumente. `DataSource.cache` ist totes Public-API.                                                                                                                                                   | datasource.js:283-294, 376-380, 415                                                           | 2  |
| ~~30~~ | Text außerhalb `{{}}` wird ungeescaped in `"…"` gesetzt: `Er sagte "Hi" {{x}}` oder Backslash am Ende → SyntaxError.                                                                                                                                                                                                                                                                                               | expression.js:157                                                                             | 1  |

## Niedrig

| #      | Problem                                                                                                                                                                                                                                                                                                  | Stelle                                               | SP |
|--------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------|----|
| ~~31~~ | `Composer.validate` (public API) auf nicht gerenderte Elemente → `object` undefined → TypeError. `select.options[selectedIndex].value` bei `selectedIndex === -1` → TypeError.                                                                                                                           | composer.js:497-580, 532, 843                        | 1  |
| 32     | `object.statics.hasOwnProperty[attribute]` (Index statt Aufruf) → Bedingung immer false, Guard wirkungslos.                                                                                                                                                                                              | composer.js:1078                                     | 1  |
| 33     | `listen()` akzeptiert `null`/`undefined` als Callback, wirft erst in `fire`. `interval` akzeptiert `""`/`0` → `setInterval(fn, NaN)` Tight-Loop.                                                                                                                                                         | composer.js:324-335, 2458-2461                       | 1  |
| 34     | Getter `PATTERN_*`/`EVENT_FILTER/NAMES` erzeugen bei jedem Zugriff neue RegExp/Arrays in Hot-Paths. Custom Selectors per `parentNode.querySelectorAll` + `includes` statt `matches()`.                                                                                                                   | composer.js:168-308, 1909-1919                       | 2  |
| 35     | `forward()` ändert weder URL noch `_history`-Ziel → `approve()`/`Path.covers()` prüft gegen reale URL und entfernt das Ziel. `Routing.locate()` crasht ohne Hash (`null.split`). `history`-Getter gibt internes Array mutierbar heraus.                                                                  | routing.js:106, 145-158, 245-259, 359-370            | 3  |
| 36     | `parent.console` ohne try/catch → SecurityError in Cross-Origin-iframe bei jedem `console.log` nach `Test.activate()`.                                                                                                                                                                                   | test.js:596-601                                      | 1  |
| 37     | `assertEquals` ≡ `assertSame` (beide `===`) trotz Doku; `typeof meta == null` immer false; Off-by-one in `Assert.create` (`index > values.length`); `clearTimeout` für `setInterval`-Handles; String-basierter Kontrollfluss auf `Timeout occurred…`; `typeValue` dispatcht `input` mit `bubbles=false`. | test.js:164, 367-368, 379-380, 643-662, 740, 826-873 | 2  |
| 38     | `Math.unique` nutzt `random % 10` (verzerrte Verteilung), `size = 0` → 16. `RegExp.quote` escaped `-` (SyntaxError mit `u`-Flag); `RegExp.escape` (ES2025) nativ verfügbar.                                                                                                                              | extension.js:351-401                                 | 1  |
| 39     | `_export` ohne Typprüfung der Zwischen-Level (primitive Globals → TypeError/stiller Fehlschlag); Export ist Wert-Snapshot (undokumentiert). Verschachtelte `(? a + (? b))` nicht unterstützt. Doku-Signatur `eval(script, [url])` widerspricht Implementierung.                                          | scripting.js:85-104, 236-262                         | 2  |
| 40     | Catch in `include` setzt `origin.innerText = "Error: …"` und greift auf `error.message` zu → TypeError bei Nicht-Error-Werten; Fehlerausgabe ersetzt Container-Inhalt. `return result` im Event-Callback wirkungslos.                                                                                    | composer.js:940-944, 1314-1320                       | 1  |
| 41     | Toter Codepfad `ANY_TYPE`/`singleNodeValue`; `transform(xmlDocument)` mit einem XMLDocument-Argument → `variants[0].replaceAll` TypeError.                                                                                                                                                               | datasource.js:143, 314-324                           | 1  |

## Summe

| Kategorie  | SP        |
|------------|-----------|
| Hoch       | 48        |
| Mittel     | 65        |
| Niedrig    | 24        |
| __Gesamt__ | __≈ 137__ |

## Einschätzung

Drei konzeptionelle Kernprobleme, aus denen viele Einzel-Findings folgen:

1. __eval als Laufzeitfundament__ (#1, #4, #13): Jede `{{}}`-Sequenz ist Code,
   keine CSP-Fähigkeit, keine Trennung von Daten und Code. Das ist eine bewusste
   Design-Entscheidung aus aspect-js, macht aber Datenquellen (i18n, XML,
   User-Content) zu Angriffsflächen. Mindestens ein Escape-Mechanismus bzw.
   Opt-out-Attribut ist Pflicht.
2. __Synchrones Laden__ (#5): Deprecated, blockiert die UI und lässt sich nicht
   nachrüsten, ohne den Render-Lock asynchron zu denken. Größter Umbau, aber
   unumgänglich.
3. __Kein Lebenszyklus-Management__ (#6, #12): Meta, Caches und Mount-Stack
   wachsen mit jedem Iterate-Zyklus. WeakMap/WeakSet ist der erste Schritt,
   Keyed-Diffing der zweite.

## Empfohlene Reihenfolge

1. __Quick Wins__ (je 1–2 SP, zusammen ~15 SP): #2, #9, #10, #22, #26, #30, #31,
   #32, #33, #36 – echte Bugs mit klarer Lösung.
2. __Sicherheit/Stabilität__: #1, #3, #7, #11, #20, #25.
3. __Architektur__: #4, #5, #6, #8, #12, #13, #21.
4. Rest nach Bedarf.
